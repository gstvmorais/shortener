import { Router } from "express";

const router = Router();

router.get("/user", null);
router.get("/user/:id", null);
router.put("/user/:id", null);
router.delete("/user/:id", null);
router.post("/user", null);

export default router;
