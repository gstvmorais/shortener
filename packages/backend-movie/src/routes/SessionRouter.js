import { Router } from "express";

const router = Router();

router.get("/session", null);
router.get("/session/:id", null);
router.put("/session/:id", null);
router.delete("/session/:id", null);
router.post("/session", null);

export default router;
